[workspace]
resolver = "2"

members = [
  "crates/wasm-echo",
  "crates/wasm-probe",
  "crates/shared-types",
  "crates/vm-runtime",
  "crates/capability-model",
]

[profile.release]
opt-level = "s"
lto = true
strip = true
